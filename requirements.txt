# Web Scraping
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
selenium==4.15.2
feedparser>=6.0.10
newspaper3k==0.2.8

# ── Local ML (CPU-only, no API key required) ───────────────────────────────
# Step 1 — Install CPU-only PyTorch (smaller download, no CUDA):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# Step 2 — Install the rest:
#   pip install -r requirements.txt
#
# Step 3 — Download models (first run only, ~250 MB, then fully offline):
#   python setup_models.py
#
# Verified working stack on Python 3.13 / Windows (CPU-only):
torch>=2.2.0                    # CPU-only build (see Step 1 above)
transformers>=4.44.0,<5.0       # Flan-T5 script + metadata generation
tokenizers==0.21.1              # Must match transformers; binary wheel on PyPI
sentencepiece>=0.2.0            # Flan-T5 tokenizer (SentencePiece)
accelerate>=0.30.0              # Speeds up CPU inference
safetensors>=0.4.1              # Fast model weight loading
regex>=2024.0.0                 # Required by transformers
yake>=0.4.8                     # Keyword extraction for YouTube tags
jellyfish>=1.0.0                # Required by yake
segtok>=1.5.0                   # Required by yake
tabulate>=0.9.0                 # Required by yake

# ── Legacy API (optional — only needed if USE_LOCAL_ML=false) ─────────────
# anthropic>=0.35.0             # Claude API — uncomment to switch back
# openai>=1.3.0                 # Unused — kept for reference only

# Video Generation
moviepy>=2.0.0
Pillow>=10.0.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.9
gTTS>=2.4.0
pyttsx3==2.90
mutagen>=1.47.0

# YouTube Upload
google-api-python-client>=2.100.0
google-auth>=2.20.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.0

# Utilities
python-dotenv>=1.0.0
schedule>=1.2.0
pyyaml>=6.0.0
rich>=13.0.0
loguru>=0.7.0
httpx>=0.25.0
flask>=3.0.0
kokoro-onnx>=0.5.0
soundfile>=0.13.1
huggingface_hub>=0.23.0

